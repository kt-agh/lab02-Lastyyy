#include <iostream>

using namespace std;

//To jest komentarz do tego pliku. Ten plik jest ewidentnie zle sformatowany
//nalezy to koniecznie poprawic, pamietajac o nazwach zmiennych
//dopuszczalnej długości linii, znakach z kodu ANSI, wcieciach i dbalości 
//o wyskoiej jakosci komentarze

int main() 
{ 
	int firstint=5; 
	int secondint=7;
	int sum=firstint+secondint;

cout <<"Suma dwoch liczb:"<<sum<<endl; // Wypisuje wynik dzialania

return 0;
}
